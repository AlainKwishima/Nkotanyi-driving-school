/** Minimum touch target (Apple HIG & Material Design). */
export const MIN_TOUCH_TARGET = 44;

/** Allow large accessibility fonts without unbounded layout explosion. */
export const MAX_FONT_SCALE_MULTIPLIER = 2;
